
        function findFirstPositive(numbers){
            return numbers.find(num => num > 0);
        }
        //Example input
        const input = [-5, -3, 0, 9, 2];
        const result = findFirstPositive(input);
        console.log(result); //output
        
    
